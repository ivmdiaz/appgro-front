export class EtiquetaModel {
    idEtiqueta: number;
    etiqueta: string;
  
    constructor(idEtiqueta: number, etiqueta: string) {
      this.idEtiqueta = idEtiqueta;
      this.etiqueta = etiqueta;
    }
  
  }
  